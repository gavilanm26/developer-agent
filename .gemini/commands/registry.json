{
  "commands": [
    {
      "trigger": "/init-agent",
      "action": "create-structure",
      "description": "Reinicia o crea la estructura base .gemini"
    },
    {
      "trigger": "/new-service",
      "action": "create-microservice",
      "description": "Crea un nuevo microservicio/feature con arquitectura hexagonal",
      "arguments": [
        { 
          "name": "language", 
          "type": "string", 
          "options": ["nestjs", "java", "python"], 
          "required": true 
        },
        { 
          "name": "name", 
          "type": "string", 
          "required": true,
          "description": "Nombre del servicio en kebab-case (ej: auth-users)"
        }
      ],
      "rulesPath": {
        "nestjs": ".gemini/rules/nestjs-hexagonal.md"
      }
    },
    {
      "trigger": "/new-module",
      "action": "create-module",
      "description": "Crea un módulo interno (feature) siguiendo arquitectura hexagonal estricta (Solo NestJS)",
      "arguments": [
        { 
          "name": "name", 
          "type": "string", 
          "required": true,
          "description": "Nombre del módulo en kebab-case (ej: payment-gateway)"
        }
      ],
      "rulesPath": ".gemini/rules/nestjs-module.md"
    }
  ]
}