export class {{FEATURE_PASCAL}}RequestDto {
  // Feature-level request DTO used by the application inbound port.
  // Add/keep only the fields needed by the use cases orchestrated by the feature service.
  [key: string]: any;
}
